interface:
  display_name: "Components"
  short_description: "Reusable UI component APIs and state coverage."
  default_prompt: "Use $system-components to design or review reusable UI components, APIs, variants, composition, states, and icon conventions."
